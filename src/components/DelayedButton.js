import React from 'react';

class DelayedButton extends React.Component {

  handleDelay = () => {
    // this.props.delay = setTimeout()
  }

  render(){
    return (
      // <button onDelayedClick={this.handleDelay} />
    )
  }
}

export default DelayedButton
